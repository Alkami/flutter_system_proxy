name: Publish

on:
  push:
    branches:
      - main
      - hotfix/**

jobs:
  build:
    name: Publish
    uses: alkami/flutter_platform/.github/workflows/reusable_publish.yaml@main
    secrets: inherit
